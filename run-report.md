# MTK setup-bootstrap v7.10.0 — run report (warp)

**Repo:** `/Users/mirkobudimir/Dev/mtk-repos-pr/warp`
**Branch:** `feat/mtk-setup-v7.10.0`
**Commit:** `d94bd0b39017bae0509f847e4c0c19106c48a1e3`
**Toolkit:** v7.10.0 (working copy `setup-eval-round1-fixes`), `CLAUDE_PLUGIN_ROOT` exported on every call.
**Mode:** fresh-generate (replace) + non-interactive (interview skipped, sensible defaults; analyzer add-on + extra tool-configs declined).

## Stack detected
- **dotnet** — markers: `src/Warp.slnx`, 17 `*.csproj`, `global.json`. Versions read verbatim: runtime libs `net10.0`; the 2 source-generator projects `netstandard2.0`; UI `src/ui/package.json` = react `^19.2.4`, vite `^8.0.1`, typescript `~5.9.3`; tests xUnit v3 `xunit.v3.mtp-v2` 3.2.2 on Microsoft Testing Platform.

## Architecture profile highlights
- This repo IS the Moberg.Warp framework: defines its own `IJob`/`IMediator`/`IRequest`/`IMessage`/`IStreamRequest` (`src/core/Warp.Core/Handlers/`); MediatR/Mediator.SourceGenerator appear ONLY in `Warp.Benchmarks` (comparison, not a dependency).
- Source-generated mediator + `[WarpHttp]` HTTP dispatch (netstandard2.0 generators).
- EF Core job store + intentional raw SQL `FOR UPDATE SKIP LOCKED` dequeue (flagged as deliberate, not an anti-pattern); snake_case via EFCore.NamingConventions; DistributedLock leader election; Cronos recurring.
- Dual first-class providers (PostgreSQL + SQL Server) under `src/core/providers/`.
- React 19 + Vite dashboard SPA (`src/ui`) over axios/REST + SignalR.
- Strict build: TreatWarningsAsErrors, NuGetAuditMode=all, StyleCop/Roslynator/Sonar/Meziantou analyzers.

## Files generated (line counts)
| File | Lines |
|---|---|
| CLAUDE.md | 83 (≤120 **PASS**) |
| AGENTS.md | 289 (over 60–120 budget — script output; see notes) |
| CODE_INDEX.md | 37 |
| .claude/references/architecture-principles.md | 99 |
| .claude/references/conventions.md | 36 |
| .claude/references/pre-commit-review-list.md | 15 (10 items) |
| .claude/rules/security.md | 10 |
| .claude/rules/architecture.md | 10 |
| .claude/rules/data-layer.md | 10 |
| .claude/rules/testing.md | 9 |
| .claude/rules/performance.md | 9 |
| .claude/rules/git-workflow.md | 8 |
| .claude/rules/project-specific.md | 9 |
| .claude/references/dotnet/coding-guidelines.md | 938 (fetched, sha256 verified) |
| .claude/detected-tools.json, settings.json, mtk-version.json | (config) |

Also shipped: dotnet refs (ef-core-checklist, performance-supplement, testing-supplement, analyzer-config) + shared refs (security-checklist, testing-patterns, performance-checklist); references.index (gitignored); .mtk-cache seed (gitignored).

## Gates fired
- **Coding-guidelines fetch:** pinned `@4043387…c3bb`, sha256 `006df483…2445` — **MATCH**.
- **verify-references (3.5a):** code-evidence paths corrected to rooted form; final = 0 genuine stale in CLAUDE.md/architecture/conventions/rules. Residual flags: (1) `.claude/skills/tech-stack-dotnet/SKILL.md` — plugin-provided skill, legitimate forward-ref; (2) 12 CODE_INDEX `path:Symbol` rows — false positives (verifier doesn't strip `:Symbol`; all 10 underlying files confirmed present via `test -f`).
- **verify-claims (3.5b):** architecture-principles = 30 claims, **0 downgrades**; conventions/CLAUDE = 0 tagged claims, 0 downgrades.
- **Secret scan (3.5c):** all generated files — **clean (rc=0)**.
- **Reference pruning (3.6):** shipped 4 dotnet refs; **pruned 1** — `mediatr-slice-patterns.md` (`tools:[mediatr]` ∩ detected = empty; repo uses its own source-gen mediator, not MediatR).
- **CLAUDE.md 120-line ceiling:** 83 lines — PASS.

## Approximated / skipped (with why)
- **Repomap:** ran `repomap.sh dotnet` → `fit: defer-to-mcp`, reason `no-treesitter-csharp` (defers to csharp-lsp MCP, not harvested). Disclosed in architecture-principles `## Provenance`; principles derived from scan recipes (grep/find).
- **Git pre-commit hook:** install **blocked by sandbox** (persistence policy). No hook existed before. Note: `.git/hooks/` is never part of the committed PR, so no PR impact. Engineer can install manually via the STEP 4 snippet.
- **Interview (2.5):** skipped (non-interactive) — rules are scan-derived only.
- **AGENTS.md:** generated by `generate-agents-md.sh` at 289 lines (script concatenates full reference bodies; exceeds the 60–120 target). It is **globally git-ignored** (`~/.gitignore_global` line 3) — the repo previously committed one, so it was force-added (`git add -f`) to faithfully replace the old config in the PR.
- **Optional add-ons declined** per runner: analyzer config, extra native tool-configs (.cursor/.windsurf/GEMINI/copilot/cline).
- **Prerequisites:** shellcheck, shfmt, dotnet-format not installed (warnings only, non-blocking).

## Honesty notes
- All `path:Symbol` entries in CODE_INDEX and all file:line evidence in architecture-principles were validated against disk; the "STALE" verifier output on CODE_INDEX is a known `:Symbol`-suffix false positive, not stale content.
- `.claude/tech-stack` shows no diff because the removed file and the regenerated one have identical content ("dotnet") — it is tracked and correct.

# Pre-Commit Review List — Moberg.Warp

> Generated by setup-bootstrap 2026-05-29. Fast checks before every commit.
> Detected triggers: EF Core, raw SQL providers, dual-provider, source generators.

1. [EF Core] `AsNoTracking` on read-only queries; `Select()` projection over `Include()` for reads.
2. [EF Core] `CancellationToken` propagated through async query methods and the worker loop.
3. [Raw SQL] New dynamic SQL is parameterized; the `FOR UPDATE SKIP LOCKED` dequeue queries are left intact (intentional).
4. [Dual-provider] Data-layer change works on BOTH PostgreSQL and SQL Server (`Warp.Provider.*`).
5. [Source gen] Mediator/HTTP changes covered by `Warp.Tests.SourceGenerator`; no runtime reflection registration introduced.
6. [Build] No new compiler/analyzer warnings (`TreatWarningsAsErrors=true` — build fails on warnings).
7. [Tests] Relational behavior tested with Testcontainers fixtures, not `UseInMemoryDatabase`.
8. No PII / full job payloads in logs.
9. Tests added for new public methods / contract changes.
10. No hardcoded secrets or credentials.

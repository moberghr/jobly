export enum JobType {
    enqueded = 1,
    scheduled = 2,
    processing = 3,
    succeeded = 4,
    failed = 5,
    deleted = 6,
    awaiting = 7,
}

export enum BatchType {
    enqueded = 1,
    scheduled = 2,
    processing = 3,
    succeeded = 4,
    failed = 5,
    deleted = 6,
    awaiting = 7,
}
